

type ResultButString = "success" | Date | Float32Array;
type Foo = { name: string; }  & { age: number };









import "./style.css";
import App from "./App.svelte";
import type { T } from "@tauri-apps/api/event-2a9960e7";

const app = new App({
  target: document.getElementById("app"),
});

export default app;
